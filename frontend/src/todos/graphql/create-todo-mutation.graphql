mutation createTodo($title: String, $content: String) {
  createTodo(input: { title: $title, content: $content }) {
    ... on ApiError {
      __typename
      errorMessage
      errorType
    }
    ... on Todo {
      id
      content
      createdAt
      title
      updatedAt
    }
  }
}
